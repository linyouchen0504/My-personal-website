[project]
requires = ["nodejs-24"]

[dev]
build = ["pip", "install", "-r", "requirements.txt"]
run = ["python", "assets/app.py"]

[deploy]
build = ["pip", "install", "-r", "requirements.txt"]
run = ["python", "assets/app.py"]
